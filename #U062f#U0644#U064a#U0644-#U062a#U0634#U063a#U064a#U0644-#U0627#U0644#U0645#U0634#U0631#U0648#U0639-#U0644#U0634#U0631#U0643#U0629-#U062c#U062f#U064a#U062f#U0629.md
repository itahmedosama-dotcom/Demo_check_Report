# دليل: تشغيل نفس المشروع لشركة جديدة (Google Sheet جديد)

كل شركة جديدة محتاجة: **شيت Google Sheets خاص بيها + مشروع Apps Script خاص بيها**، وربطهم بملف `js/config.js`. الملفات (HTML/CSS/JS) نفسها بتفضل زي ما هي، مش محتاجة أي تعديل في الكود.

---

## 1️⃣ اعمل Google Sheet جديد
- افتح [sheets.google.com](https://sheets.google.com) واعمل شيت (Spreadsheet) فاضي جديد.
- سيبه فاضي تمامًا، الشيتات الداخلية (`Reports`, `Users`, `Settings`, `PendingUsers`) هتتعمل لوحدها أول مرة.

## 2️⃣ اربط Apps Script بالشيت
- من داخل الشيت: **Extensions (الإضافات) > Apps Script**.
- امسح أي كود موجود في `Code.gs` والصق مكانه محتوى ملف `Code-gs.txt` بالكامل.
- **غيّر قيمة المفتاح** في أول سطر عشان تبقى فريدة للشركة دي (مش لازم تفضل زي المشروع القديم):
  ```js
  const API_KEY = 'اسم-الشركة-2026-مفتاح-سري';
  ```

## 3️⃣ اعمل نشر (Deploy)
- **Deploy > New deployment**.
- اختار النوع: **Web app**.
- Execute as: **Me**.
- Who has access: **Anyone**.
- دوس **Deploy**، ووافق على الصلاحيات (Authorize access) لو طلب منك.
- هيديك رابط ينتهي بـ `/exec` — **انسخه**، ده اللي هتحتاجه في الخطوة الجاية.

⚠️ أي تعديل تعمله بعد كده في `Code.gs` لازم يتبعه: **Deploy > Manage deployments > ✏️ > Version: New version > Deploy** (نفس الرابط بيفضل شغال، بس لازم تعمل "نشر جديد" عشان التعديل يتفعّل).

## 4️⃣ حدّث ملف `js/config.js`
افتح `js/config.js` وغيّر القيمتين دول بالرابط والمفتاح بتوع الشركة الجديدة:
```js
const DEFAULT_API_URL = 'الرابط_اللي_نسخته_من_خطوة_3';
const DEFAULT_API_KEY = 'نفس_المفتاح_اللي_كتبته_في_Code.gs';
```
⚠️ لازم القيمتين يكونوا **مطابقين حرفيًا 100%** بين `Code.gs` و`config.js`، وإلا هيرجع خطأ `unauthorized`.

## 5️⃣ ضيف أول حساب أدمن يدويًا في شيت `Users`
الموقع مش هيقدر يضيفلك أول أدمن لوحده (عشان تدخل صفحة `users.html` أصلًا لازم تكون داخل كأدمن). يبقى أول مرة بس بتضيفه يدوي في الشيت:

- افتح شيت `Users` (هيتعمل لوحده أول ما تفتح `users.html` أو تدي لي علم أعمله يدوي).
- تأكد إن صف العناوين (الصف الأول) فيه بالظبط:
  ```
  username | full_name | password | role | status | idle_time | warning_time | phone | employee_id | email | subscription_start | subscription_end
  ```
- ضيف صف تاني بالبيانات دي:

| العمود | القيمة |
|---|---|
| username | اسم المستخدم اللي تختاره |
| full_name | الاسم اللي هيظهر |
| password | **هاش SHA-256** لكلمة السر (مش الباسورد كنص عادي) — قولّي الباسورد اللي عايزه وهحسبهولك |
| role | `admin` |
| status | `active` |
| idle_time | `30` |
| warning_time | `5` |
| phone / employee_id / email | اختياري |
| subscription_start / subscription_end | سيبهم فاضيين (مش مطبقين على الأدمن) |

## 6️⃣ ارفع الملفات على الاستضافة
- لو نفس المستودع (Repository) على GitHub: اعمل **مجلد فرعي منفصل للشركة الجديدة** وحط فيه كل الملفات (مع `config.js` المعدّل)، أو اعمل **مستودع GitHub Pages منفصل تمامًا** للشركة دي (الأنضف).
- بعد الرفع، commit + push، وهيشتغل الموقع على رابط GitHub Pages الخاص بيه.

## 7️⃣ خصّص هوية الشركة (اختياري)
بعد ما تسجل دخول بحساب الأدمن اللي عملته، روح لصفحة **الإعدادات** (`users.html`) وغيّر من قسم **"🎨 هوية الشركة"**:
- اسم الشركة
- الشعار
- الألوان (أساسي / ثانوي / تمييز)

كل ده بيتخزن في شيت `Settings` الخاص بالشركة دي، من غير أي تعديل تاني في الكود.

## 8️⃣ اختبر
- افتح رابط الموقع، جرب تسجل دخول بحساب الأدمن.
- لو مش شغال، افتح الرابط ده في المتصفح للتشخيص السريع:
  ```
  <API_URL>?sheet=users&key=<API_KEY>
  ```
  لازم يرجّعلك array فيه بيانات المستخدمين، مش `{"error":"unauthorized"}`.

---

### ملخص سريع: كل شركة جديدة = 
✅ Google Sheet جديد → ✅ Apps Script + Deploy جديد → ✅ `config.js` مُحدّث → ✅ صف أدمن يدوي في `Users` → ✅ رفع الملفات على استضافة (نفس المشروع أو مستودع منفصل).
