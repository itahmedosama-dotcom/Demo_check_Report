// ============================================================
// إعدادات الاتصال بـ Google Sheets عبر Apps Script
// ⚠️ تحذير: هذا الملف يظهر بالكامل لأي زائر للموقع (لأنه HTML/JS ثابت).
// أي حد يقدر يشوف الرابط والمفتاح من كود الصفحة ويستخدمهم مباشرة.
// ============================================================
// القيم الافتراضية (بتتفعّل لكل زوار الموقع). لو عايز تغيّرها بشكل دائم
// لكل المستخدمين، استخدم قسم "🔗 ربط Google Sheet" في صفحة الإعدادات
// (أدمن بس) وحمّل ملف config.js الجديد بعد التعديل، وارفعه بدل الملف ده.
// ============================================================
const DEFAULT_API_URL = 'https://script.google.com/macros/s/AKfycbz-4JqJpcVIrSvCGP65Xd3odrz6ZWi5yui9aKeZN5b41vhzWJN4QP_NFTpUJLUQOUTV_Q/exec';
const DEFAULT_API_KEY = 'Nsh2026SecretKeyXyz';

// ------------------------------------------------------------
// "معاينة محلية" فقط: لو الأدمن استخدم زرار "تطبيق ومعاينة على هذا الجهاز"
// في صفحة الإعدادات، بيتخزن رابط/مفتاح مؤقت في localStorage بتاع المتصفح ده
// بس (مش هيؤثر على أي زائر تاني للموقع) عشان يقدر يجرّب رابط شيت جديد
// قبل ما يقرر يفعّله فعليًا لكل الموقع عن طريق تحميل واستبدال هذا الملف.
// ------------------------------------------------------------
const API_URL = (typeof localStorage !== 'undefined' && localStorage.getItem('nsh_override_api_url')) || DEFAULT_API_URL;
const API_KEY = (typeof localStorage !== 'undefined' && localStorage.getItem('nsh_override_api_key')) || DEFAULT_API_KEY;
